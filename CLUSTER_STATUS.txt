Last updated: 2026-08-26

Latest completed jobs/profile: Selena jobs 2897846 and 2898432 completed the
former fixed-grid full univariate profile. The analyzed snapshot contains
Electricity, Solar, Traffic, and Exchange Rate at 168:24, 336:48, and 504:168,
seed 1 and stride 512. Chronos-2 beat the best baseline on all nine hourly
Electricity/Solar/Traffic configurations; mean nMSE improvements by dataset
were 31.335%, 43.099%, and 37.394%, respectively.

Current scientific contract: settings are cadence-specific. Hourly datasets
use 168:24, 336:48, 504:168; daily datasets use 7:1, 14:2, 30:7; and 15-minute
datasets use 96:4, 192:8, 672:96. Full univariate, controls, and covariates
include Electricity, Traffic, Solar, Exchange Rate, and every feasible dataset
listed in datasets/time/catalog.json. Full foundation uses its five primary
datasets plus feasible TIME datasets at the cadence-specific mid and long
ranges. Reports aggregate comparable short/mid/long ranges while retaining
literal-setting tables. Schema 1 is unchanged.

Reuse/rerun decision: completed Electricity, Solar, and Traffic univariate
configurations remain current and will be skipped by an exact resubmission.
The completed Exchange Rate configurations used hourly-scale settings and are
excluded from current evidence; rerun Exchange Rate at the three daily ranges.
No TIME evaluation has completed, so every selected TIME task is new. The old
full report mixes cadences and must be regenerated after those tasks finish.
The Electricity 504:168 test identity remains current for all four families.

Running or queued jobs: not re-queried by this local protocol update. Earlier
test launches may continue safely because their Electricity 504:168 identity
did not change.

Next cluster actions: synchronize this code to Selena, verify that the prepared
datasets root exposes time/catalog.json, then resubmit
`EXPERIMENT_MODE=full sbatch slurm/selena/main/01_univariate_selena.slurm`.
It should reuse the three hourly datasets, run daily Exchange Rate and TIME,
and regenerate the full report. After the outstanding test jobs complete,
full controls, covariates, and foundation workflows are eligible to launch;
they will use the same automatic TIME discovery. Use DATASETS_OVERRIDE for a
narrow recovery run; an override replaces automatic TIME selection.

Known job IDs: 2897846, 2898432 (completed former full-univariate launches).
