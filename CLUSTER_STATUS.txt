Last updated: 2026-08-27

Latest synchronized inventory: 76 completed manifests (60 univariate,
9 controls, 3 covariates, and 4 foundation), 2 manifests still marked running,
and 1 interrupted foundation manifest. Every completed manifest predates the
current CSV-only input contract and records prepared tensor inputs. Controls
also used the former additive exclusion behavior. Current-contract completed
evidence therefore remains zero.

Current scientific contract: settings are cadence-specific. Hourly datasets
use 168:24, 336:48, 504:168; daily datasets use 7:1, 14:2, 30:7; and 15-minute
datasets use 96:4, 192:8, 672:96. Full univariate, controls, and covariates
include Electricity, Traffic, Solar, Exchange Rate, and every feasible dataset
listed in datasets/time/catalog.json. Full foundation uses its five primary
datasets plus feasible TIME datasets at the cadence-specific mid and long
ranges. Reports aggregate comparable short/mid/long ranges while retaining
literal-setting tables. Schema 1 is unchanged.

Reuse/rerun decision: no synchronized manifest may be reused for a current
computation identity. Rerun the test boundary for all four families after the
current code and CSV dataset configuration are synchronized. Then rerun the
cadence-aware full scope, including Exchange Rate at its three daily ranges and
all feasible TIME datasets. Regenerate reports only from current identities.

Running or queued jobs: controls job 2910738 and covariates job 2910739 each
left one now-obsolete removed-model manifest marked running. DGX foundation job 45673 is marked
interrupted after Chronos-2 weights were unavailable. Query scheduler state
before using the supported lifecycle recovery; do not edit manifests directly.

Next cluster actions: publish and synchronize the current code, verify the CSV
dataset root and time/catalog.json, query jobs 2910738 and 2910739, and resolve
any terminal stale manifests through the workflow. Run the four test families
before submitting cadence-aware full profiles. Use DATASETS_OVERRIDE only for
a narrow recovery run; an override replaces automatic TIME selection.

Known job IDs: 2897225, 2897846 (historical univariate); 2898430 (historical
controls); 2898431 (historical covariates); 2897232, 2897857 (historical
foundation); 2910738, 2910739 (manifests marked running); 45673 (interrupted).
