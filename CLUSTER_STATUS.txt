Last updated: 2026-08-28

CURRENT SCHEDULER STATE
- The full Selena foundation-model workflow was submitted on 2026-08-28.
  Its scheduler state and job ID have not yet been synchronized locally.
- The latest synchronized publication is commit eef9edce and contains Selena
  logs for jobs 2955373, 2955374, 2955375, and 2955400 plus lightweight test
  reports. Detailed run manifests were not published in this commit.

LATEST JOB OUTCOMES
- sunivar, job 2955373, full profile: the evaluation stage visited all 945
  configurations (713 exact completed runs reused and 232 new runs made
  artifact-ready). The final full-report `srun` failed with `Pathname of a
  file, directory or other parameter too long`; no full univariate report was
  produced. Do not rerun evaluation.
- scontrols, job 2955374, full profile: the evaluation stage visited all 1,512
  configurations (546 reused and 966 new artifact-ready). The final report
  failed with the same overlong-parameter error; no full controls report was
  produced. Do not rerun evaluation.
- scovars, job 2955375, full profile: the evaluation stage visited all 756
  configurations (423 reused and 333 new artifact-ready). The final report
  failed with the same overlong-parameter error; no full covariates report was
  produced. Do not rerun evaluation.
- sfoundation, job 2955400, test profile: successful. All four foundation
  configurations were available (Chronos-2 and Chronos-Bolt reused;
  Chronos-T5 and TS-ICL completed), and
  outputs_selena/reports/foundation_models/test/results.csv plus its report
  manifest were generated.

INTERPRETATION
- The three full evaluation sweeps reached their complete configuration lists;
  only aggregate reporting failed. The local fix now passes exact task
  selection through a short task-file argument instead of expanding 189 task
  pairs on the `srun` command line. Deploy it to Selena before report-only
  resubmission.
- The earlier Chronos-T5 checkpoint/configuration failure is resolved by job
  2955400. The foundation-model full profile is now eligible to run.
- CUDA determinism warnings and Chronos-T5 horizon warnings were non-fatal.

NEXT JOBS
1. Observe the submitted full foundation-model workflow and record its job ID
   and scheduler state. If its batch shell started before this fix reached
   Selena, keep its completed evaluations and rerun only its report afterward.
2. Deploy the report task-file fix to Selena.
3. `STAGES=report EXPERIMENT_MODE=full sbatch slurm/selena/main/01_univariate_selena.slurm`
4. `STAGES=report EXPERIMENT_MODE=full sbatch slurm/selena/main/02_controls_selena.slurm`
5. `STAGES=report EXPERIMENT_MODE=full sbatch slurm/selena/main/03_covariates_selena.slurm`

After each terminal job, pull results to DGX and publish the exact logs plus
lightweight reports. Use detailed synchronization only when manifest-level or
per-window diagnosis is required.
