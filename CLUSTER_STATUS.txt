Last updated: 2026-08-29

CURRENT SCHEDULER AND ARTIFACT STATE
- No live scheduler state is synchronized locally.
- All four full TSFM result grids are complete and pulled from Selena. Every
  task has the expected model/control variants and every aggregate nMSE is
  finite.
- Full report-only jobs 2964366 (univariate), 2964367 (controls), and 2964368
  (covariates) completed on 2026-08-28. The covariate report used the old
  reference logic; its raw rows are valid but its derived comparisons were
  empty.
- Foundation replacement job 2964473 resumed the failed workflow, completed
  Weather, Exchange Rate, and the remaining eligible TIME tasks, and wrote the
  full foundation report. The earlier partial report from jobs 2962543 and
  2964370 is superseded.

FULL ARTIFACT COVERAGE
- Univariate: 945 rows = 189 tasks x five models over 63 datasets.
- Controls: 1,512 rows = 189 tasks x eight model/control combinations over 63
  datasets.
- Covariates: 756 rows = 189 tasks x four backbone/covariate combinations over
  63 datasets.
- Foundation models: 512 rows = 128 tasks x four models over 64 datasets. This
  grid includes Weather at the mid and long settings; each Weather
  configuration records replacement of 21 missing values.

REPORT STATE
- The univariate, control, and foundation aggregate reports are current.
- The covariate `results.csv`, `plot_index.csv`, and `report_manifest.json` are
  retained as valid result/provenance artifacts. Six empty or header-only
  comparison products from the old report were removed locally on 2026-08-29.
- The corrected covariate report must compare each identity-covariate row with
  the same backbone under `covariate_mode=none`. The local fixed code produces
  756 comparison rows from the retained raw results, including 378 identity
  rows.
- `scripts.migrate_report_diagnostics` only moves legacy `plots/` and
  `averaged_inputs/` directories into `diagnostics/`; it does not rebuild
  comparison tables.

CURRENT INTERPRETATION
- Chronos-2 beats the best simple baseline on 153 of 189 univariate tasks, ties
  one, and has a 24.10% median relative nMSE improvement. Persistence remains
  better on all three Exchange Rate settings.
- Instance normalization and constant-window removal have zero median effect;
  neither control supports a universal intervention.
- Identity covariates beat the same no-covariate backbone on 185 of 189
  Chronos-2 tasks and 176 of 189 TS-ICL tasks, with median improvements of
  97.93% and 98.07%. Identity is an oracle capability control, not a deployable
  method.
- Across the complete foundation grid, TS-ICL wins 51 tasks, Chronos-2 50,
  Chronos-Bolt 17, and Chronos-T5 10. Chronos-2 has the best mean rank and
  beats TS-ICL head-to-head on 69 of 128 tasks.

NEXT JOB
1. Ensure the fixed TSFM code is synchronized to Selena, then run only:
   `STAGES=report EXPERIMENT_MODE=full sbatch slurm/selena/main/03_covariates_selena.slurm`
2. Pull the lightweight covariate report and confirm that
   `nmse_configurations.csv` contains 756 comparison rows and the aggregate
   tables are nonempty. No inference or evaluation rerun is required.

RUNTIME ROOTS
- DGX: project-local outputs/ and logs/.
- Selena: /scratch/users/<lowercase-nni>/codes/tsfm/outputs_selena and
  logs_selena.
- Lightweight synchronization is the default; detailed sync is required only
  for per-window or manifest-level diagnosis.
